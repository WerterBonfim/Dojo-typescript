{
  "name": "em-nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --require ts-node/register --inspect-brk index.ts",
    "test": "ts-node jest.startup.ts",
    "test:debug": "node --require ts-node/register --inspect-brk jest.startup.ts",
    "poker-test": "node --require ts-node/register jest.startup.ts poker"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "jest": "^25.5.2",
    "jest-cli": "^25.5.2",
    "ts-jest": "^25.4.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "ts-node": "^8.9.1"
  }
}
